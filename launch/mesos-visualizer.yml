run:
  type: docker
env:
  - CLUSTER
resources:
  cpu: .1
  max_mem: .1
expose:                # list of ports to expose
  - name: http         # identifier for this port mapping
    port: 8888         # port to map to inside the container
    bind: 80           # NOTE: value ignored by mesos
